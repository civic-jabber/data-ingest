CREATE TABLE IF NOT EXISTS civic_jabber.legislators (
  id text,
  name text,
  given_name text,
  family_name text,
  gender text,
  current_state text,
  current_party text,
  current_district text,
  current_chamber text,
  links text,
  image text,
  twitter text,
  capitol_email text,
  district_email text,
  as_of_date timestamp
) ;
